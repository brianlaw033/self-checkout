
import { getDistance } from 'self-checkout/helpers/get-distance';
import { module, test } from 'qunit';

module('Unit | Helper | get distance');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getDistance([42]);
  assert.ok(result);
});

