{{#if login.logedin}}
  <div class= "container">
    <header>
      <nav class="navbar navbar-default">
        <div class="container-fluid">
           <div class="navbar-header">
             <a class="navbar-brand">{{login.currentUser}}</a>
           </div>
          <ul class="nav navbar-nav">
            <li>{{#link-to 'store-index' login.userId}}My Store{{/link-to}}</li>
            <li>{{#link-to 'sales' login.userId}}Sales{{/link-to}}</li>
            <li><button class='logout-store' type='button'{{action "logout"}}>Logout</button></li>
          </ul>
        </div>
      </nav>
    </header>
      {{outlet}}
  </div>
{{/if}}
